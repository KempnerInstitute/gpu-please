[project]
name = "aws-terraform"
version = "0.1.0"
description = "CLI tool to provision AWS GPU EC2 instances via Terraform with optional post-provisioning recipe installation"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.42.81",
    "pyyaml>=6.0.3",
    "requests>=2.33.1",
    "rich>=14.3.3",
]
